{% extends 'base.html.twig' %}

    {% block title %}Nouvelle réclamation{% endblock %}

    {% block body %}
  {% if app.session.get('user') and app.session.get('user').idrole == 2 %}
    {% include 'backandfront/Front/navbar.html.twig' %}
{% elseif app.session.get('user') and app.session.get('user').idrole == 3 %}
    {% include 'backandfront/Front/navbarfree.html.twig' %}
{% endif %}

 <div style="margin: 60px"> 
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1>Créer une nouvelle réclamation</h1>
        <a href="{{ path('app_reclamation_index') }}" class="btn btn-primary">Retour à la liste</a>
    </div>
    {{ include('reclamation/_form.html.twig') }}
    <div>
    <br>
  {# <button type="submit" class="btn btn-primary">{{ button_label|default('Envoyer') }}</button> #}
  </div>
    </div>   
    
  <br>
    {{ include ('backandfront/Front/footer.html.twig') }}
    
{% endblock %}
