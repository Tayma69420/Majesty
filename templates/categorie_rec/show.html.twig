{% extends 'base.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block body %}
{% include 'frontandback/back/NiceAdmin/navbarmod.html.twig' %}
 <div style="margin: 60px">
    <div class="d-flex justify-content-between align-items-center mb-3">
    <h1>Catégorie des Réclamations</h1>
        <a href="{{ path('app_categorie_rec_index') }}" class="btn btn-primary">Retour à la liste</a>
    </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_rec.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ categorie_rec.type }}</td>
            </tr>
        </tbody>
    </table>
    <div class="mt-5">
        <a href="{{ path('app_categorie_rec_edit', {'id': categorie_rec.id}) }}" class="btn btn-outline-primary mr-2">Modifier</a>
<button class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">Supprimer</button>
    {{ include('categorie_rec/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
