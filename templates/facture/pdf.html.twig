<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture</title>
    <style>
       body {
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;
  background-color: #f2f2f2;
}

h1 {
  text-align: center;
  margin: 20px 0;
}

table {
  border-collapse: collapse;
  margin: 0 auto;
  background-color: #fff;
  box-shadow: 0 0 10px rgba(0,0,0,0.2);
}

td {
  padding: 10px;
  border: 1px solid #ccc;
}

td:first-child {
  font-weight: bold;
  background-color: #f2f2f2;
}

td:last-child {
  text-align: right;
}

tr:last-child td {
  font-weight: bold;
  background-color: #f2f2f2;
}

    </style>
</head>
<body>
    <h1>Facture</h1>
    <table>
        <tr>
            <td>Nom:</td>
            <td>{{ facture.firstname }}</td>
        </tr>
        <tr>
            <td>Prénom:</td>
            <td>{{ facture.lastname }}</td>
        </tr>
        <tr>
            <td>Numéro de carte:</td>
            <td>{{ facture.cardnumber }}</td>
        </tr>
        <tr>
            <td>Total:</td>
            <td>{{ facture.total }} €</td>
        </tr>
        <!-- Add whatever other information you want to include in the PDF here -->
    </table>
</body>
</html>